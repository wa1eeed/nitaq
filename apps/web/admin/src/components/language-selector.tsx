'use client';
// Language selector — Arabic is the sole supported language (v0.9.20).
// Kept as a stub so import paths don't break; renders nothing.
export function LanguageSelector() {
  return null;
}
